apiVersion: v1
kind: ConfigMap
metadata:
  name: prescription-service-config
  namespace: healthsync
data:
  NODE_ENV: "production"
  LOG_LEVEL: "info"
  REDIS_URL: "redis://redis:6379"
  KAFKA_BROKERS: "kafka:29092"
  MQTT_BROKER: "tcp://emqx:1883"
  PORT: "3004"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prescription-service
  namespace: healthsync
  labels:
    app: prescription-service
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: prescription-service
  template:
    metadata:
      labels:
        app: prescription-service
        version: v1
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: prescription-service
          image: healthsync/prescription-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3004
              protocol: TCP
          envFrom:
            - configMapRef:
                name: prescription-service-config
          env:
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: healthsync-secrets
                  key: jwt-secret
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: healthsync-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: healthsync-secrets
                  key: redis-url
            - name: CORS_ORIGINS
              valueFrom:
                secretKeyRef:
                  name: healthsync-secrets
                  key: cors-origins
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
---
apiVersion: v1
kind: Service
metadata:
  name: prescription-service
  namespace: healthsync
  labels:
    app: prescription-service
spec:
  selector:
    app: prescription-service
  ports:
    - name: http
      port: 3004
      targetPort: 3004
      protocol: TCP
  type: ClusterIP
