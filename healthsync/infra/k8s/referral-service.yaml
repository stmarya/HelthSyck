apiVersion: v1
kind: ConfigMap
metadata:
  name: referral-service-config
  namespace: healthsync
data:
  NODE_ENV: "production"
  LOG_LEVEL: "info"
  REDIS_URL: "redis://redis:6379"
  KAFKA_BROKERS: "kafka:29092"
  MQTT_BROKER: "tcp://emqx:1883"
  PORT: "3006"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: referral-service
  namespace: healthsync
  labels:
    app: referral-service
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: referral-service
  template:
    metadata:
      labels:
        app: referral-service
        version: v1
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: referral-service
          image: healthsync/referral-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3006
              protocol: TCP
          envFrom:
            - configMapRef:
                name: referral-service-config
          env:
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: healthsync-secrets
                  key: jwt-secret
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: healthsync-secrets
                  key: database-url
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 3006
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3006
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
---
apiVersion: v1
kind: Service
metadata:
  name: referral-service
  namespace: healthsync
  labels:
    app: referral-service
spec:
  selector:
    app: referral-service
  ports:
    - name: http
      port: 3006
      targetPort: 3006
      protocol: TCP
  type: ClusterIP
